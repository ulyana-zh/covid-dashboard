const state = {
  isGlobal: true,
  currentCountry: null,
  currentListMode: 'cases',
  searchValue: '',
  searchedList: null,
  currentList: null,
  allCountriesList: null,
};

export default state;
